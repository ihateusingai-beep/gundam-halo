import { MissionSelect } from "@/components/gundam/MissionSelect";

/** Overview page — currently renders MissionSelect (cockpit standby view). */
export function OverviewPage() {
  return <MissionSelect />;
}
