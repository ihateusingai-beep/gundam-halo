"""Live2D trigger layer — M3.

Decouples the LLM from Live2D. The agent emits emotion tags in a
small DSL (`[EMO:awakening]`); the responder translates them into
Live2D expression + motion pairs.

M1: interface only. M3: ship NT-D / Unicorn theme responder.
"""
